# `@zhizhu/playwright-shell-contract`

`browser_profile_slug` 与可选 `default_start_path`/`defaultStartPath` 的校验（与 `apps/client` registry、`apps/api/src/devicePlaywrightShellSync.ts` **单一事实来源**）。

Build: `npm run build`（产出 `dist/`）。
