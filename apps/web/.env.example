# 复制本文件为 apps/web/.env（已被 gitignore）后联调。
# 或在仓库根执行 **`npm run bootstrap:env`** 自动补上 `VITE_API_BASE_URL`、`VITE_CONSOLE_PUBLIC_REGISTER` 等（不重写已有键）。
# PostgreSQL 连接串在仓库根 `.env`（见根目录 `.env.example`），仅服务端使用；勿写进 VITE_*。
#
# 置空或不设置时：使用内置 mock 数据（演示租户 demo）
# 本地联调 apps/api：与 `npm run dev -w @zhizhu/api` 端口一致；根 `.env` 的 CORS_ORIGIN 须与本页访问 origin 一致
# VITE_API_BASE_URL=http://127.0.0.1:3000
#
# 是否显示登录页「注册」入口。未设或非 true 时不展示注册链。
# 须与仓库根 .env 的 CONSOLE_ALLOW_PUBLIC_REGISTER 一致：根目录未设 true 时 POST /api/v1/auth/register 为 403；老环境可在根 .env 补 CONSOLE_ALLOW_PUBLIC_REGISTER=true 或再执行 npm run bootstrap:env（仅补缺失键）。
# 数据库迁移须含 047（npm run migrate:api），否则租户管理相关 API 可能异常。
# VITE_CONSOLE_PUBLIC_REGISTER=true
#
# VITE_API_BASE_URL=https://api.example.com
#
# 设备绑定页「下载客户端」按钮；须 http/https，与 Electron 客户端 ZHIZHU_RELEASES_PAGE_URL 填同一发布页即可
# VITE_ZHIZHU_RELEASES_PAGE_URL=https://example.com/releases

# 可选：拉长 mock 请求时间以观察加载态
# VITE_MOCK_LATENCY_MS=300
