# 复制为仓库根目录的 `.env` 后填写真实值；`.env` 已被 .gitignore 忽略，勿提交。
# PostgreSQL 仅给服务端（如 apps/api）使用，不要写进 apps/web 的 VITE_*。
#
# 环境约定：此处应填写**专门用于开发/测试**的实例与 database（或本地 Postgres）。
# `npm run migrate:api` 会在该库执行建表与演示种子（含 021_console_extensions / 022_seed_console_extensions）；**勿**把生产库连接串放进本机 `.env`。
#
# --- 联调清单（与 apps/web 配合）---
# 1) 在此配置 DATABASE_URL 或 PG* 后执行：npm run migrate:api
# 2) 在 apps/web/.env 配置 VITE_API_BASE_URL（如 http://127.0.0.1:3000），与下方 PORT 一致
# 3) CORS_ORIGIN 必须与浏览器打开前端的 origin 完全一致（127.0.0.1 与 localhost 不同源）
# 4) 强鉴权联调时设置 JWT_SECRET；登录页会 POST /api/v1/auth/token 换取 Bearer

# 推荐：阿里云 RDS 等一行连接串（密码中的特殊字符需 URL 编码）
# DATABASE_URL=postgresql://zhizhu:YOUR_PASSWORD@rm-xxxx.pg.rds.aliyuncs.com:5432/zhizhu?sslmode=require

# 或拆分为（与 ORM 二选一即可）
# PGHOST=rm-xxxx.pg.rds.aliyuncs.com
# PGPORT=5432
# PGDATABASE=zhizhu
# PGUSER=zhizhu
# PGPASSWORD=YOUR_PASSWORD
# PGSSLMODE=require
# 若报错 “The server does not support SSL connections”，先删除 PGSSLMODE 或改为：PGSSLMODE=disable

# 可选：本地 API 端口（apps/api）
# PORT=3000

# 启用后：POST /api/v1/auth/login 或 /auth/token（带 email+password）签发 JWT；所有 /api/v1/tenants/:id/* 须 Authorization: Bearer，且 tid 与路径租户一致。
# 投放写入另校验角色 tenant_admin 或 ad_placement:write。未设置时保持本地开发无鉴权。
# JWT_SECRET=请使用足够长的随机串（建议 hex：openssl rand -hex 32）
# Web 联调：配置 VITE_API_BASE_URL 后，登录页 POST /api/v1/auth/login，body 用 login_identifier 填「用户名或邮箱」换票；注册须 username + email（见 README）。设备页可连 WS /api/v1/ws?token=…
#
# 已配置 JWT_SECRET 时，默认禁止 POST /api/v1/auth/token 仅凭 tenant_id 换票；仅本地开发可开启：
# CONSOLE_ALLOW_DEV_TENANT_TOKEN=true

# --- 可选：邮件 SMTP（供后续验证码/邀请/通知等；控制台「系统设置 → 邮件 SMTP」页可查看是否已配置）---
# 配置在运行 apps/api 的环境，勿写入 apps/web 的 VITE_*。
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_FROM=noreply@example.com
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_SECURE=false

# 浏览器来源 CORS（须与浏览器地址栏 origin 完全一致；Vite 默认多为 http://localhost:5173）
# 前端 fetch 使用 credentials:include，API 会返回 Access-Control-Allow-Credentials: true
# 未设 CORS_ORIGIN 时默认含 5173；Vite 端口被占用会改用 5174/5175…：此时不必逐端口配置，
# 只要**未**将下面 CORS_STRICT 设为 1，@zhizhu/api 会接受「本机 localhost / 127.0.0.1 / ::1 上任意端口的 http(s)」作为 Origin。
# 生产/固定来源时建议设 CORS_STRICT=1，且只列明 CORS_ORIGIN（可逗号分隔多域）。
# CORS_ORIGIN=http://localhost:5173
# 若你固定用 http://127.0.0.1:5173 打开前端，则改为该地址
# CORS_ORIGIN=http://127.0.0.1:5173
# 与「仅白名单、禁止本机多端口通配」时可打开（此时须把实际前端 origin 写进 CORS_ORIGIN）
# CORS_STRICT=1

# --- 可选：Electron 客户端（apps/client）---
# 置 1 时启动壳自动打开渲染进程 DevTools（排障；可写在本文件或 `apps/client/.env`，主进程启动时会通过 dotenv 加载）
# ZHIZHU_CLIENT_DEVTOOLS=1
# 未设置时：若 ZHIZHU_WEB_BASE_URL 为本地 5173，则 API 默认推为 http://127.0.0.1:3000/（与 PORT 一致）
# ZHIZHU_API_BASE_URL=http://127.0.0.1:3000/
# 保存租户 ID 时客户端会请求 GET {ZHIZHU_API_BASE_URL}api/v1/tenant-registry/:tenantId 校验库内是否存在该租户（须 API 已 migrate 且可连）
# 绑定成功后可选：完整 WSS URL（须含 /api/v1/ws?token=… 与登录换得的 JWT），用于开发心跳；未配置则客户端不连 WSS
# ZHIZHU_WSS_URL=ws://127.0.0.1:3000/api/v1/ws?token=YOUR_JWT
