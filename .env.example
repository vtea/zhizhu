# 复制为仓库根目录的 `.env` 后填写真实值；`.env` 已被 .gitignore 忽略，勿提交。
#
# 本地开箱：在项目根目录执行 **`npm run bootstrap:env`** 可自动补足**缺失**键（不覆盖已有值），并生成：
# `JWT_SECRET`、`DEVICE_TOKEN_SECRET`、示例 `DATABASE_URL`（仅当既无 `DATABASE_URL`、也未配齐 PGHOST+PGUSER+PGDATABASE）、`PORT`、`CONSOLE_ALLOW_DEV_TENANT_TOKEN`、
# **`CONSOLE_ALLOW_PUBLIC_REGISTER`**（本地默认 true，与 Web 侧 `VITE_CONSOLE_PUBLIC_REGISTER` 成对），
# 并在 `apps/web/.env` 写入 `VITE_API_BASE_URL` 等（不覆盖已有键）。仍须在本机 Postgres 中建库或按需改连接串后再 **`npm run migrate:api`**。
#
# **老环境未跑过新迁移时**：务必执行 **`npm run migrate:api`** 直至包含 **`047_biz_platform_tenant_entitlement`**；否则租户列表/登记可能降级或报「缺列」类错误。
#
# **自助注册**：未设置 **`CONSOLE_ALLOW_PUBLIC_REGISTER=true`** 时，`POST /api/v1/auth/register` 返回 **403**。若根目录 `.env` 尚无该键，可手写补上，或再执行 **`npm run bootstrap:env`**（仅补缺失键）。须与 `apps/web/.env` 的 **`VITE_CONSOLE_PUBLIC_REGISTER`** 一致。
# apps/api 读取连接：若存在 **`DATABASE_URL` 则优先生效**，与拆行 **`PGHOST`+…** 勿混用两套且互相冲突。
#
# PostgreSQL 仅给服务端（如 apps/api）使用，不要写进 apps/web 的 VITE_*。
#
# 环境约定：此处应填写**专门用于开发/测试**的实例与 database（或本地 Postgres）。
# `npm run migrate:api` 会在该库执行建表与演示种子（含 021_console_extensions / 022_seed_console_extensions，以及租户授权列 **`047`**）；**勿**把生产库连接串放进本机 `.env`。
#
# --- 联调清单（与 apps/web 配合）---
# 1) 在此配置 DATABASE_URL 或 PG* 后执行：npm run migrate:api（须含 **047**，见上）
# 2) 在 apps/web/.env 配置 VITE_API_BASE_URL（如 http://127.0.0.1:3000），与下方 PORT 一致
# 3) CORS_ORIGIN 必须与浏览器打开前端的 origin 完全一致（127.0.0.1 与 localhost 不同源）
# 4) 强鉴权联调时设置 JWT_SECRET；登录页会 POST /api/v1/auth/token 换取 Bearer

# --- 必读：PostgreSQL 连接只允许「二选一」，勿两套同时解开注释 ---
# 若使用下面「拆行 PG*」，则必须 **注释整行 DATABASE_URL**，否则 `@zhizhu/api` 仍会优先走 DATABASE_URL，导致你以为在用 PG* 却始终连示例库。
#

# 推荐：阿里云 RDS 等一行连接串（密码中的特殊字符需 URL 编码）
# DATABASE_URL=postgresql://zhizhu:YOUR_PASSWORD@rm-xxxx.pg.rds.aliyuncs.com:5432/zhizhu?sslmode=require

# 或拆分为（与 ORM 二选一即可）
# PGHOST=rm-xxxx.pg.rds.aliyuncs.com
# PGPORT=5432
# PGDATABASE=zhizhu
# PGUSER=zhizhu
# PGPASSWORD=YOUR_PASSWORD
# PGSSLMODE=require
# 若报错 “The server does not support SSL connections”，先删除 PGSSLMODE 或改为：PGSSLMODE=disable

# 可选：本地 API 端口（apps/api）
# PORT=3000

# API 监听地址（apps/api）。默认 127.0.0.1（仅本机访问，适合前贴 Nginx）。Docker / 需容器外直连时设为 0.0.0.0。
# API_LISTEN_HOST=127.0.0.1
# API_LISTEN_HOST=0.0.0.0

# 可选：视频封面落盘根目录（apps/api）。未设置时默认用仓库根下 storage/video-covers/（按 租户-账户-视频 分子目录）。
# ZHIZHU_VIDEO_COVER_STORAGE=/absolute/path/to/covers

# 启用后：POST /api/v1/auth/login 或 /auth/token（带 email+password）签发 JWT；所有 /api/v1/tenants/:id/* 须 Authorization: Bearer，且 tid 与路径租户一致。
# 投放写入另校验角色 tenant_admin 或 ad_placement:write。未设置时保持本地开发无鉴权。
# JWT_SECRET=请使用足够长的随机串（建议 hex：openssl rand -hex 32）
# Web 联调：配置 VITE_API_BASE_URL 后，登录页 POST /api/v1/auth/login，body 用 login_identifier 填「用户名或邮箱」换票；注册须 username + email（见 README）。
# Electron 设备绑定：须单独配置 **DEVICE_TOKEN_SECRET**（与 JWT_SECRET 分开），用于签发 `device_access_token`；WSS `/api/v1/ws?token=` 可为用户 JWT 或设备凭证。
# DEVICE_TOKEN_SECRET=请使用与 JWT_SECRET 不同的随机串（如 openssl rand -hex 32）

# 已配置 JWT_SECRET 时，默认禁止 POST /api/v1/auth/token 仅凭 tenant_id 换票；仅本地开发可开启：
# CONSOLE_ALLOW_DEV_TENANT_TOKEN=true

# 是否开放 POST /api/v1/auth/register 自助注册。
# - 未设置或不为 true 时：**自助注册 API 固定 403**（老仓库若根 .env 从未加过此项，请补上 `=true`，或执行 `npm run bootstrap:env` 仅补缺失键）。
# - 须与 apps/web 的 VITE_CONSOLE_PUBLIC_REGISTER 一致；否则一端显示注册、另一端 403。
# - 生产环境建议关闭；本地 bootstrap 会为缺失项默认补 true。
# CONSOLE_ALLOW_PUBLIC_REGISTER=true

# --- 可选：邮件 SMTP（供后续验证码/邀请/通知等；控制台「系统设置 → 邮件 SMTP」页可查看是否已配置）---
# 配置在运行 apps/api 的环境，勿写入 apps/web 的 VITE_*。
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_FROM=noreply@example.com
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_SECURE=false

# 控制台 Web 在浏览器中的公开访问根地址（**不要**尾斜杠）。用于向成员发送邮件时拼接登录页链接（/login）。
# 须与员工实际打开前端的地址一致（如 https://console.example.com 或 http://127.0.0.1:5173）；仅服务端读取，勿写入 apps/web 的 VITE_*。
# CONSOLE_WEB_PUBLIC_URL=http://127.0.0.1:5173

# 浏览器来源 CORS（须与浏览器地址栏 origin 完全一致；Vite 默认多为 http://localhost:5173）
# 前端 fetch 使用 credentials:include，API 会返回 Access-Control-Allow-Credentials: true
# 未设 CORS_ORIGIN 时默认含 5173；Vite 端口被占用会改用 5174/5175…：此时不必逐端口配置，
# 只要**未**将下面 CORS_STRICT 设为 1，@zhizhu/api 会接受「本机 localhost / 127.0.0.1 / ::1 上任意端口的 http(s)」作为 Origin。
# 生产/固定来源时建议设 CORS_STRICT=1，且只列明 CORS_ORIGIN（可逗号分隔多域）。
# CORS_ORIGIN=http://localhost:5173
# 若你固定用 http://127.0.0.1:5173 打开前端，则改为该地址
# CORS_ORIGIN=http://127.0.0.1:5173
# 与「仅白名单、禁止本机多端口通配」时可打开（此时须把实际前端 origin 写进 CORS_ORIGIN）
# CORS_STRICT=1

# --- 可选：Electron 客户端（apps/client）---
# 置 1 时启动壳自动打开渲染进程 DevTools（排障；可写在本文件或 `apps/client/.env`，主进程启动时会通过 dotenv 加载）
# ZHIZHU_CLIENT_DEVTOOLS=1
# 未设置时：若 ZHIZHU_WEB_BASE_URL 为本地 5173，则 API 默认推为 http://127.0.0.1:3000/（与 PORT 一致）
# ZHIZHU_API_BASE_URL=http://127.0.0.1:3000/
# 第三方控制台 origin（如抖音线索后台），供 Runner 拼接 `goto.path`；优先级低于任务 payload / 文件规则 meta.console_base
# ZHIZHU_CONSOLE_BASE_URL=https://leads.cluerich.com
# 保存租户 ID 时客户端会请求 GET {ZHIZHU_API_BASE_URL}api/v1/tenant-registry/:tenantId 校验库内是否存在该租户（须 API 已 migrate 且可连）
# 绑定成功且 client-state 写入 deviceAccessToken 后：WSS 基址（路径须为 /api/v1/ws，**不要**手写 ?token=）；主进程从本机状态拼 `device_access_token`；未配置则客户端不连 WSS
# ZHIZHU_WSS_URL=ws://127.0.0.1:3000/api/v1/ws
# 客户端「关于与环境」标签页中「打开发布页」的一键地址（须 http/https）；未设置时仍可「检查更新」查看当前版本说明
# ZHIZHU_RELEASES_PAGE_URL=https://example.com/releases
# Web 控制台设备绑定页「下载客户端」按钮（构建 apps/web 时写入 VITE_*）；建议与上一行填同一发布页
# VITE_ZHIZHU_RELEASES_PAGE_URL=https://example.com/releases
#
# Runner / Playwright：首启或通过菜单「Runner Playwright 自检」前，会自动执行 `playwright install chromium`（写入 userData 标记，同源版本不重复下载）。正式安装包已预置 Node 22 与 Chromium（见 scripts/stage-client-pack-deps.mjs）。以下可选：
# ZHIZHU_PACK_NODE_VERSION=22.14.0
# ZHIZHU_SKIP_PLAYWRIGHT_AUTO_INSTALL=1
# ZHIZHU_FORCE_PLAYWRIGHT_INSTALL=1
# ZHIZHU_PLAYWRIGHT_BROWSERS_PATH=/path/to/playwright/cache
# headed-login（可视化浏览器）：设为 1 时在 Runner 子进程 stderr 输出拟真 UA/视口预览 JSON（不向 UI 透出）
# ZHIZHU_PW_FINGERPRINT_DEBUG=1
# task-persistent（任务 Runner spawn）：为 1 或 true 时使用 headless 持久化上下文（仍与 headed-login 同源指纹）
# ZHIZHU_PW_TASK_HEADLESS=1
# 用户在弹窗中选「不再提示此版本」时写入 userData/runner-decline-chromium-prompt.json（与同锁定的 Playwright 版本绑定）
