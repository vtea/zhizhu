---
description: 排障与修改须基于证据、可验证；禁止用猜测与弱因果表述糊弄过关
alwaysApply: true
---

# 确定性排障与交付（本仓库）

处理 bug、配置异常、自动化/入库问题时遵守：

1. **先证据后结论**：用日志行、HTTP 状态、堆栈、数据库字段、代码路径等**可复核事实**定位根因；**禁止**用「可能 / 大概 / 或许 / 大概率」代替查证。
2. **修改必须落地**：能改代码或配置则**直接修改**并写入仓库；**禁止**只给「试试看」的操作指南了事（除非环境权限在本机以外且已写明阻塞项）。
3. **禁止猜测掩盖**：信息不足时**明确列出缺失项与读取位置**（哪个接口、哪张表、哪个 env），再补齐后再改；**禁止**用推测填空凑结论。
4. **验收可验证**：交付时说明**改了哪些文件**、**为何该改动必然对应根因**、**如何验证已修复**（命令、页面路径或断言）。

对用户/评审的书面说明：优先陈述**已证实的事实 + 已做的修改 + 验证方式**；避免弱因果与模糊措辞。
