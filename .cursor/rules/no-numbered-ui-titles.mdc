---
description: UI 标题与导航不得使用「1、2、3」序号角标或前缀
alwaysApply: true
---

# 禁止在标题与菜单中使用数字序号

在 **Web 控制台（`apps/web`）** 与 **Electron 客户端（`apps/client`）** 的可视 UI 中：

- **不得**在页面分块标题、侧栏菜单、顶栏 Tab、面包屑等**导航/结构文案**旁使用圆形角标、`1` `2`、`Step 1` 式编号，或把「第几步」作为主导视觉（用户强烈反感且易与数据混淆）。
- **`SectionCard`** 已移除 `index` prop；**不要恢复**或另写同类「标题左侧数字徽章」组件来绕开规则。
- **例外**：仅当数字是**业务数据本身**时使用（例如表格列「序号」、工单编号、分页页码）。

新功能占位标题用**纯语义名称**即可（如「当前组织概览」「企业主体」），不要用编号表达顺序。
